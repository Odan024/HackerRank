import numpy

#Tranforma una matris o arreglo de caracteres en uno numerico depende como se se especimique en dtype=
A = numpy.array(['1','2','3'],dtype=int)

print(A)

# se pueden hacer inifinidades de operaciones con los numpy.array